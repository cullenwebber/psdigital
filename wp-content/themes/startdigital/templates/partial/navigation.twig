<header class="sticky top-0 bg-white z-50 py-4">
	<div data-navigation class="container z-20 w-full flex items-center justify-between">
		<a href="{{ site.url }}" rel="home">{% include 'icons/logo.twig' %}</a>
		<nav class="max-lg:h-6">
			{% include 'menu.twig' with {
				items: menu.get_items
			} %}
		</nav>
	</div>
</header>
